# PostgreSQL Database Configuration
# Update these values with your local PostgreSQL credentials

# Database Host (usually localhost for local development)
VITE_DB_HOST=localhost

# Database Port (default PostgreSQL port is 5432)
VITE_DB_PORT=5432

# Database Name (create this database in PostgreSQL)
VITE_DB_NAME=portfolio_db

# Database User (default is usually 'postgres')
VITE_DB_USER=postgres

# Database Password (your PostgreSQL password)
# IMPORTANT: Set this to your actual PostgreSQL password
VITE_DB_PASSWORD=your_password_here

# JWT Secret for Authentication
# IMPORTANT: Change this to a secure random string (at least 32 characters)
# Generate one using: openssl rand -base64 32
VITE_JWT_SECRET=change_this_to_a_secure_random_string_at_least_32_characters_long

# Gemini API Key (for AI features)
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Production Database Settings (uncomment for production)
# VITE_DB_SSL=true
# VITE_DB_MAX_CONNECTIONS=20
# VITE_DB_IDLE_TIMEOUT=30000
# VITE_DB_CONNECTION_TIMEOUT=2000

# Notes:
# 1. Copy this file to .env and update the values
# 2. Never commit .env file to version control
# 3. For production, use environment-specific values
# 4. Generate a strong JWT secret using: openssl rand -base64 32
